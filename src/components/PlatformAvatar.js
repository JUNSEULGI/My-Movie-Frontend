import React from 'react';
import styled from '@emotion/styled';
import { Avatar } from '@mui/material';

export const OTTLogo = styled(Avatar)`
  padding: 4px;
  margin-bottom: 16px;

  /* background-color: gray; */
`;
